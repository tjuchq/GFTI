# 使用版本化模拟产物复现算法统计

均匀模拟评测使用版本化 `samples.csv` 保存可复算答案，使用 `summary.json` 保存算法版本、随机规则、题目指纹和生效歌曲参数快照。这样既保留 CSV 处理大规模表格数据的便利，也避免把嵌套复现上下文重复写入每一行；算法调整前后可以使用同一模拟样本集重新计算并比较，而不会把随机波动误认为算法变化。

## Consequences

交换格式升级时必须增加 `schema_version`，评测公式、计分方式或排序规则变化时必须更新 `algorithmVersion`。损坏或未知版本的文件会整份拒绝，不会跳过错误行后继续统计。
