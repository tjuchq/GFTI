# Agent instructions

## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues for `yelanyanyu/GFTI`; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repository; read the root `CONTEXT.md` and `docs/adr/` when they exist. See `docs/agents/domain.md`.
